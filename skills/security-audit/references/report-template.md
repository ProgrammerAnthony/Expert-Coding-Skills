# 安全审计报告模板

使用以下模板输出最终审计报告：

---

```markdown
# 安全审计报告

**项目**：[项目名称]
**审计日期**：[YYYY-MM-DD]
**扫描模式**：[Quick / Standard / Deep]
**技术栈**：[如：Python 3.11 + Django 4.2 + PostgreSQL]
**审计范围**：[目录或模块列表]

---

## 执行摘要

[2-3 句话概述：项目安全状况总体评级（优/良/差）、最严重的问题类别、核心修复建议]

**风险评级**：[Critical / High / Medium / Low / Informational]

---

## 漏洞统计

| 严重度 | 数量 |
|--------|------|
| Critical | X |
| High | X |
| Medium | X |
| Low | X |
| 合计 | X |

---

## 漏洞详情

### [VULN-001] [漏洞标题]

- **严重度**：Critical / High / Medium / Low
- **维度**：D1 注入 / D2 认证 / ... / D10 供应链
- **位置**：`src/module/file.py:45`
- **CWE**：CWE-89（SQL 注入）[可选]

**问题描述**：
[清晰描述漏洞的性质和产生原因]

**漏洞代码**：
```python
# 文件：src/api/search.py，第 45 行
query = f"SELECT * FROM items WHERE name = '{keyword}'"  # 危险！
cursor.execute(query)
```

**数据流路径**：
```
Source: request.GET.get('keyword')   [第 42 行]
  → 变量 keyword [第 42 行]
  → SQL 字符串拼接 [第 45 行]
Sink: cursor.execute(query)          [第 46 行]
净化：无
```

**利用条件**：
[是否需要登录、特定权限、特定场景]

**影响**：
[可能造成的数据泄露、权限提升、拒绝服务等后果]

**修复建议**：
```python
# 使用参数化查询
cursor.execute("SELECT * FROM items WHERE name = %s", (keyword,))
```

---

### [VULN-002] [下一个漏洞]

[同上格式...]

---

## 待人工验证项

以下疑似问题未能通过静态分析完整确认，需要动态测试或更多上下文：

| # | 文件:行号 | 疑似问题 | 验证方向 |
|---|----------|----------|----------|
| 1 | `src/api/user.py:88` | 可能存在越权 | 测试 A 用户是否能访问 B 用户数据 |

---

## 已有安全措施（亮点）

[记录项目中做得好的安全实践，避免开发团队认为报告只挑毛病]

- 密码使用了 `bcrypt` 哈希存储
- 登录接口有失败次数限制（5次/分钟）
- 统一使用 ORM 参数化查询（除上述发现的问题）

---

## 修复优先级建议

| 优先级 | 漏洞 | 建议时间 |
|--------|------|----------|
| P0 立即修复 | VULN-001, VULN-003 | 24 小时内 |
| P1 尽快修复 | VULN-002 | 本迭代内 |
| P2 计划修复 | VULN-004, VULN-005 | 下个迭代 |

---

## 未覆盖范围

由于 [时间限制 / 扫描模式限制]，以下方面未纳入本次审计：

- 动态运行时检测（需运行测试套件）
- 第三方服务安全配置（如 OSS、消息队列）
- 社会工程学攻击面
```

---

## 快速输出模板（Quick 模式）

```markdown
## 快速安全扫描报告

**扫描时间**：约 [X] 分钟
**高危发现**：[数量] 个

### 高危问题
1. **[位置]**：[问题简述]
   修复：[一行建议]

### 敏感信息暴露
[如有]

### 已知 CVE
| 依赖 | 版本 | CVE | 影响 |
|------|------|-----|------|

### 建议下一步
[ ] 对高危问题进行 Standard 扫描验证
[ ] 更新存在 CVE 的依赖
```
